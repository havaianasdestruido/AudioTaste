# Secrets — never commit real credentials
.env

# Generated from .env / Vercel env by scripts/build-config.mjs
config.js

# Node
node_modules/

# Vercel deploy output (generated on the build machine, never committed)
dist/

# Build artifacts (regenerate via scripts/parse_scrobbles.mjs)
res/build/

# Personal listening history export — keep out of the public repo
res/UltimateQuack.csv

# Vulnerability/audit reports — internal only, never public
logs/
